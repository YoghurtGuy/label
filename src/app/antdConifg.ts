import type{ ConfigProviderProps } from "antd";
export const antdConfig: ConfigProviderProps = {
    theme: {
        components: {
            Layout: {
                headerBg:"#ffffff"
            },
        },
    }
    }